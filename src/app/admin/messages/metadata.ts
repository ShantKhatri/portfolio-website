import type { Metadata, Viewport } from 'next';

export const metadata: Metadata = {
  title: 'Admin Messages | Portfolio',
  description: 'Manage incoming contact messages',
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
};